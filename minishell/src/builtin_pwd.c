
#include "minishell.h"

char	builtin_pwd(t_command *command)
{
	/* char	**args;
	char	fd;

	args = command->args;
	if (command->pipe_out == 0)
		fd = 1; */
	/* else
		fd = xd; */
	//write_strs(, newline, fd);
	return (0);
}
